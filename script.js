//is it wright or wrong for blue:
function choice1(id) {
    var x = " " // correct or incorrect:
    document.location = "/" //<--- next question / what file to go next
    alert(x);
}
//is it wright or wrong for red:
function choice2() {
        var x = " " // correct or incorrect:
          document.location = "/" //<--- next question / what file to go next
    alert(x);
}
